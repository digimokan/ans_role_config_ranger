# keymap for switching to console CLI mode
keymap_enter_console_cli_mode: ';'

# keymap for exiting console CLI mode
keymap_exit_console_cli_mode: '<esc>'

# call a shell cmd using the current entry as the arg
keymap_shell_cmd_on_current_entry: 'w'

# call a shell cmd
keymap_shell_cmd: 'a'

