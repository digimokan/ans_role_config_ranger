# paths used for jumps and special settings (see downloads.yml, entry_trash.yml)
ranger_root_dir: "/"
ranger_mount_dir: "/mnt"
ranger_media_dir: "/media"
ranger_fileserver_dir: "/net"
ranger_user_home_dir: "/home/{{ ranger_user_name }}"
ranger_user_documents_dir: "{{ ranger_user_home_dir }}/Documents"
ranger_user_desktop_dir: "{{ ranger_user_home_dir }}/Desktop"
ranger_user_download_dir: "{{ ranger_user_home_dir }}/Downloads"
ranger_user_music_dir: "{{ ranger_user_home_dir }}/Music"
ranger_user_pictures_dir: "{{ ranger_user_home_dir }}/Pictures"
ranger_user_videos_dir: "{{ ranger_user_home_dir }}/Videos"
ranger_user_trash_dir: "{{ ranger_user_home_dir }}/.trash"
ranger_user_devel_dir: "{{ ranger_user_home_dir }}/Devel"

